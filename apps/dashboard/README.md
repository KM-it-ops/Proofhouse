# Dashboard (prototype)

Vite + React visual prototype. It is not Simple Mode, not Developer Mode,
and not a product surface (DFR-002, REJ-007). It reads
`loadouts/legendary_loadouts.json` only; it does not own IR semantics.
This is a prototype, not the product.
Do not demo it as Proofhouse.
