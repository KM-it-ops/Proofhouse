"""Offline optimize surfaces: the reviewed model registry and its package data.

Nothing in this package reaches the network. The registry under ``data/`` is the
single source for every human-facing model list in the repository.
"""
